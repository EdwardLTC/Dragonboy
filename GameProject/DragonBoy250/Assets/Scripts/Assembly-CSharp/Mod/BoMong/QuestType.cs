namespace Mod.BoMong.QuestHandler
{
	public enum QuestType
	{
		TrainMonster, // Nhiệm vụ đánh quái thường
		TrainGold,    // Nhiệm vụ tự sát lụm vàng
		KillPlayer,   // Nhiệm vụ pem người
		KillLurcher   // Nhiệm vụ ăn trộm (bỏ qua)
	}
}
